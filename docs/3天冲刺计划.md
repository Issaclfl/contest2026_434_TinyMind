# TinyMind-Audio · 3 天收口计划（9/17 → 9/20）

> 状态：**已批准执行**。本文件是当前唯一有效的作战计划，
> 取代 `4天冲刺计划.md`（那份基于旧假设：有队友、板子可用、赛道为新硬件适配）。
> 作品名：**TinyMind-Audio** ｜ 提交包：`TinyMind-TinyMind-Audio-contest2026_434_TinyMind.zip`

---

## 一、定位（已确认）

**一句话**：为 openvela 的 SF32LB52 平台补上缺失的板载音频子系统（NuttX audio
lower-half 驱动），让这块出厂"哑巴"的板子能录音、能放音。

**诚实边界**：本阶段交付**端侧语音交互的底座**。AI 应用（语音唤醒 / 相框）
写进"后续工作"，不假装做了。

**为什么这样定**：赛道是「AI 硬件产品创新」，理想情况下应有 AI 产品形态；
但 3 天、1 人、板子未组装齐备的现实下，硬塞 KWS 模型只会让所有部分都变半成品。
交一个**每部分都完整且可验证的底座**，比交一个**处处半成品的"AI 产品"**更可信。

---

## 二、赛道口径（已更正）

- 赛道：**AI 硬件产品创新**（不是新硬件平台适配 —— 这是早期理解错误）
- 技术难度 30 分的官方描述仍含"openvela **新芯片适配与底层驱动扩展**" →
  驱动工作**照常计分**，但定位是"底座"而非"作品"
- 硬性要求：图形/AI/多媒体三者至少落地一项（我们实际有两项：openvela LVGL 图形
  + 我们的音频多媒体）；含语音唤醒则必须用「你好，openvela」（本轮无语音唤醒，
  该条不适用）；Skill 沉淀 ≥1（已完成）；源码须提交到 `dev-ai-contest-2026` 分支

---

## 三、P0 必修（已完成 通过）

三个会让 3 天全废的问题，全部在 9/17 上午修掉并验证：

| # | 问题 | 状态 |
|---|------|------|
| ① | **设备注册路径错** → 真机上根本无音频设备 | 已修（defconfig +2 行，调用改裸名） |
| ② | **`cat /dev/audio0` 是无效验证** | 已改用 `nxrecorder`，全仓文档排查干净 |
| ③ | `packages/` 树断连 → 自研 app 不可行 | 已探明结论，写入报告路线图 |

详细根因与修法见 `board/sf32lb52_audio/README.md` 第七节。

---

## 四、3 天日程

### Day 1（9/17，今天）
- **上午** 修 P0 三项 → 全新配置构建成功（flash 8.65% / sram 16.91%）→ 提交仓
- **下午（你操作）**：不接屏烧录 → 串口 → `ls /dev` 期望 `audio0` → `nxrecorder` 采一段
- **晚上**：不依赖硬件的并行项 —— AI 日志导出、GitHub fork + CLA + push

### Day 2（9/18）
- 有数据：录实测数据（文件大小 ≈ 32000 B/s × 秒数）、写报告主体
- 无数据：M3 调试迭代（预计 2–4 轮；我改代码，你看串口日志）
- **每次失败现象与修法逐字记录** ← 这就是报告里"适配难点逐条记录"的素材

### Day 3（9/19）
- 报告终稿 + 视频（串口实测 + 代码讲解，不依赖屏幕）+ 打包上传
- 确认源码已在 GitHub fork 的 `dev-ai-contest-2026` 分支

---

## 五、明确砍掉（写进报告"后续工作"）

KWS 语音唤醒、AI 相框 UI、GPU 推理加速、LVGL 应用界面。

并且把已探明的工程结论写进去：`packages/` 树在本工程中**未接入构建**
（`packages/CMakeLists.txt` 不存在 → `apps/CMakeLists.txt:78-80` 的
`if(EXISTS ...)` 不成立），应用层接入需先补 `packages/CMakeLists.txt` +
`packages/Kconfig` 两道"总闸"。这是真实的调研产出，不是搪塞。

---

## 六、止损线

- **每晚 22:00** 检视：若 M3 仍无数据，次日转"编译验证 + 设计说明"的诚实版本
- 排线/喇叭到不了货**不影响**底座交付（录音不用屏、不用喇叭）
- 押金约束：必须提交作品才能退 50 元押金并留下板子
